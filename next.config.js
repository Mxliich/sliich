/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  basePath: '',
  assetPrefix: '',
  images: {
    domains: ['sliich.vercel.app'],
  },
}

module.exports = nextConfig 
